{% trans_default_domain 'FOSUserBundle' %}

{% if error %}
    <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
{% endif %}


  

   


        <div id="login">
        <aside>
            
            <div class="title" style="text-align:center;border-bottom: 1px solid #ededed;
  margin: -45px -60px 25px -60px;
  padding: 20px 60px 25px 60px;height:auto;">
                <h4 style="margin-bottom:0;">Se connecter</h4>
                <img  src="{{asset('frontFolder/img/lg.png')}}" >
             

                 <div style="margin-top:5px;margin-bottom:5px;">
                {% if error %}
    <div class="alert alert-danger alert-dismissible"> {{ error.messageKey|trans(error.messageData, 'security') }}</div>
               {% endif %}


 



  {% for  message in app.session.flashbag.get('successs') %}
  
        <div class="alert alert-success alert-dismissible">
            <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
           <div class="text-center "> {{ message }} </div>
        </div>
 
{% endfor %}



 {% for  message in app.session.flashbag.get('noticeErreur') %}
  
        <div class="alert alert-danger alert-dismissible">
            <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
           <div class="text-center "> {{ message }} </div>
        </div>
 
{% endfor %}







 </div>       


            </div>




              <form action="{{ path("fos_user_security_check") }}" method="post">
               
               
                   
                     {% if csrf_token %}
        <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
                    {% endif %}

                <div class="form-group">
                    <label>Identifiant</label>
                    <input type="text"  name="_username" value="{{ last_username }}" required="required" class="form-control"  placeholder="Identifiant" >
                   
                </div>
                <div class="form-group">
                    <label>Mot de passe</label>
                      <input type="password" name="_password" class="form-control" required="required" placeholder="Mot de passe"   >

                   
                </div>
                <div class="clearfix add_bottom_30">
                    <div class="checkboxes float-left">
                        <label class="container_check">Remember me
                          <input type="checkbox">
                          <span class="checkmark"></span>
                        </label>
                    </div>
                    <div class="float-right mt-1"><a id="forgot" href="javascript:void(0);">J'ai oublié Mot de passe</a></div>
                </div>
                <button type="submit" class="btn_1 rounded full-width">Connexion </button>
                <div class="text-center add_top_10"> J'ai n'ai pas de compte sur lieuxTournage.tn <br><strong><a href="{{path('fos_user_registration_register')}}">Je m'inscris gratuitement</a></strong></div>
            </form>
            <div class="copy text-center">© SQLI Services</div>
        </aside>
    </div>










